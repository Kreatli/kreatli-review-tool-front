/**
 * Horizontal rule matching Storyblok `.wysiwyg hr` (Wysiwyg.module.scss).
 */
export function GuideSectionRule() {
  return (
    <hr className="my-0 w-full border-0 border-t border-foreground-300" aria-hidden />
  );
}
